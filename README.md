# SurfKit

A toolkit to build GUI surfer AI agents

## Installation

```sh
pip install surfkit
```

## Usage

Create a new agent

```sh
surfkit init [NAME]
```

Run the agent

```sh
surfkit run . --provider kubernetes
```

Publish the agent

```sh
surfkit publish .
```
